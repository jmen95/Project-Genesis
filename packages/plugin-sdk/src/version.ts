/** SDK public API version — semver for plugin-sdk releases. */
export const SDK_API_VERSION = '0.1.0';

/** Discovery manifest filename used by the plugin kernel. */
export { PLUGIN_MANIFEST_FILE, PLUGIN_API_VERSION } from '@genesis/plugin-kernel';
