# {{PLUGIN_ID}}

Minimal Genesis plugin created with `@genesis/create-plugin`.

## Build

```bash
pnpm install
pnpm build
```

## Load locally

```bash
export GENESIS_PLUGIN_PATH=$(pwd)
genesis plugin list
```

Capabilities are inferred from contributions — do not add `capabilities` to `genesis.plugin.json`.
